import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  container: {
    height: 8,
    width: "100%",
    backgroundColor: "#fff",
    overflow: "hidden",
  },
  bar: {
    height: "100%",
    backgroundColor: "#000",
  },
});

export default styles;
